// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.akash.nsvue"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
